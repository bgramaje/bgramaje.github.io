# Implementation Plans

Generated by the `improve` skill (deep audit) on 2026-07-11 against commit
`5fffbdf`. Execute in the order below unless dependencies say otherwise. Each
executor: read the plan fully before starting, honor its STOP conditions, and
update your row when done.

The audit was read-only; no source code was modified. Recon facts that apply
to every plan: Vite 6 + React 18 + TypeScript 5.6 (strict, `noUnusedLocals`/
`noUnusedParameters`) + Tailwind v3 + shadcn (new-york, no CSS vars). Build is
`npm run build` (`tsc -b && vite build`); lint is `npm run lint` (`eslint .`).
Use the `@/` alias for `src/` imports. Use terminal tokens (`text-terminal-*`,
`bg-terminal-*`) over raw hex. No new deps for one-liners.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Make `npm run build` green again (install `@types/mdx`) | P1 | S | — | TODO |
| 002  | Delete unused components and dead exports | P2 | S | 001 | TODO |
| 003  | Remove unused direct dependencies | P3 | S | 002 | TODO |
| 004  | Standardize on the `motion` package (drop `framer-motion`) | P2 | M | 001 | TODO |
| 005  | Clear the lint baseline to zero errors | P2 | S-M | 002, 004 | TODO |
| 006  | Add focus trap and focus restore to the modals | P3 | S-M | 001 | TODO |
| 007  | Remove the artificial loading delay in BlogPage | P3 | S | — | TODO |
| 008  | Fix stale path references in README.md | P3 | S | — | TODO |
| 009  | Reduce npm audit advisories (dev/build-time only) | P3 | S | 003, 004 (preferred) | TODO |
| 010  | Add a `projects` terminal command | P2 | S | 001 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- **001 is the gate.** `npm run build` is currently broken (`tsc -b` exits 1
  on the missing `mdx/types` module → ~30 cascading implicit-any errors).
  Plans 002, 004, 005, 006, 010 all use `npm run build` as a verification
  gate, so 001 must land first. CI (`npm run build` in
  `.github/workflows/deploy.yml`) is failing on every push until 001 lands.
- **002 before 003 and 005.** Deleting `toolbar.tsx` removes 8 of 18 lint
  errors and confirms `react-syntax-highlighter` is unused; deleting
  `code-block.tsx` dead exports shrinks the surface before dep removal.
- **004 before 005.** The `dot-pattern.tsx` lint fix in 005 is independent of
  the import specifier, but doing 004 first means `dot-pattern` already
  imports `motion/react` and 005 touches only the unused-var issue.
- **006, 007, 008, 010 are independent** of each other and can run in any
  order after 001 (010 needs a green build to verify).
- **009 last** is preferred so the audit reflects the final manifest (after
  003 removes dead deps and 004 drops `framer-motion`); it is not a hard
  dependency.

## Findings considered and rejected

- **`Date.now()` as React key in `HomePage`/`TerminalModal` history items
  (`src/pages/HomePage.tsx:24,96`, `src/components/terminal/TerminalModal.tsx:53,85`)**:
  potential key collision if two commands run in the same millisecond. Not
  worth a plan — collisions are invisible in practice (React dedupes by key,
  worst case is a dropped warning) and the fix (a monotonic counter) touches
  hot path state for no user-visible gain. Revisit only if history bugs appear.
- **`BitcoinTicker` swallows fetch errors silently
  (`src/components/BitcoinTicker.tsx:75`)**: the `.catch(() => {})` is correct
  for a non-critical decorative ticker; surfacing errors would add noise.
  Not worth doing.
- **`MermaidDiagram` separate from `mdx-mermaid`**: resolved by deletion in
  Plan 002 (it is dead code), not by reconciling the two implementations.
- **Adding a test framework + `processCommand` unit test**: flagged as a
  direction option but not selected this round. The pure `processCommand`
  function is the one piece where a ~40-line vitest test would pay for
  itself; deferred until the maintainer wants a test baseline.
- **Tailwind v3 → v4 / React 18 → 19 migration**: explicitly out of scope per
  AGENTS.md ("Do not migrate to Tailwind v4 or React 19 without an explicit
  request"). Not audited as a finding.

## Notes on audit coverage

- Read directly (no subagents — repo is 5.7K LOC across 61 TS/TSX files): all
  page-level components, the command router, both MDX loaders, the portfolio
  data module, both modal components, `code-block.tsx`, `mdx-components` /
  `mdx-work-components`, `BitcoinTicker`, `Snowfall`, `MermaidDiagram`,
  `JobsOutput`, `HelpOutput`, `SkillsOutput`, `ticker.tsx`, `dot-pattern.tsx`,
  `toolbar.tsx`, `Layout.tsx`, `App.tsx`, `vite.config.ts`, `tsconfig.json`,
  `eslint.config.js`, `package.json`, CI workflow, README.
- Spot-checked via lint/grep only (vendored registry/shadcn primitives, low
  churn): `badge`, `button`, `card`, `avatar`, `popover`, `collapsible`,
  `rainbow-button`, `highlighter`, `light-rays`, `dotted-map`,
  `logo-carousel`, `scroll-progress`, `morphic-navbar`, `work/*`,
  `terminal/*` subcomponents, `Callout`, `CommandChips`, `CommandToolbar`.
- No ADR / design / PRD docs exist in the repo (only agent skills under
  `.agents/skills/`), so there were no decided tradeoffs to honor or stale
  decisions to flag.
